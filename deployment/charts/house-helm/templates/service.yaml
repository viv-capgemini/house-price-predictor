apiVersion: v1
kind: Service
metadata:
  name: {{ include "house-helm.fullname" . }}
  labels:
    {{- include "house-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "house-helm.selectorLabels" . | nindent 4 }}
