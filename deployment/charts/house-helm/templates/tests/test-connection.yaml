apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "house-helm.fullname" . }}-test-connection"
  labels:
    {{- include "house-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "house-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
